% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/actinet.R
\name{actinet}
\alias{actinet}
\title{Run Actinet Model on Data}
\usage{
actinet(
  file,
  outdir = tempfile(),
  classifier = NULL,
  sample_rate = NULL,
  model_path = NULL,
  pytorch_device = NULL,
  no_hmm = FALSE,
  require_sleep_above = NULL,
  single_sleep_block = FALSE,
  force_download = FALSE,
  exclude_first_last = NULL,
  exclude_wear_below = NULL,
  csv_start_row = NULL,
  csv_txyz = NULL,
  csv_txyz_idxs = NULL,
  csv_date_format = NULL,
  calibration_stdtol_min = NULL,
  plot_activity = FALSE,
  cache_classifier = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{file}{accelerometry file to process, including CSV,
CWA, GT3X, and \code{GENEActiv} bin files}

\item{outdir}{folder location to save output files}

\item{classifier}{Enter custom activity classifier file to use. Default: walmsley (Walmsley2020 annotations of activity intensity). Can also enter path to local classifier (.joblib.lzma) file.}

\item{sample_rate}{Sample rate for measurement, otherwise inferred.}

\item{model_path}{the file path to the model.  If on disk, this can be
re-used and not re-downloaded.  If \code{NULL}, will download to the
temporary directory}

\item{pytorch_device}{torch device to use, e.g.: 'cpu' or 'cuda:0'. Default: 'mps' if available, otherwise 'cpu'}

\item{no_hmm}{Disable HMM post-processing}

\item{require_sleep_above}{Require sleep blocks to exceed a minimum duration, otherwise be classified as sedentary. Pass values as strings, e.g.: '2H', '30min'. Default: None (no requirement)}

\item{single_sleep_block}{Recognize only one sleep block per day, all other sleep blocks will be converted to sedentary}

\item{force_download}{Force download of classifier file}

\item{exclude_first_last}{{first,last,both} Exclude first, last or both days of data. Default: None (no exclusion)}

\item{exclude_wear_below}{Exclude days with wear time below threshold. Pass values as strings, e.g.: '12H', '30min'. Default: None (no exclusion)}

\item{csv_start_row}{Row number to start reading a CSV file. Default: 1 (First row)}

\item{csv_txyz}{CSV_TXYZ Column names for time, x, y, z in CSV files. Comma_ separated string. Default: 'time,x,y,z'}

\item{csv_txyz_idxs}{Column indices for time,x,y,z (0_indexed, e.g., '0,1,2,3'). Overrides csv_txyz.}

\item{csv_date_format}{Date time format for csv file when reading a csv file. See https://docs.python.org/3/library/datetime.html#strftime_and_strptime_format_codes for more possible codes. Default: '\%Y-\%m-\%d \%H:\%M:\%S.\%f' (e.g. '2023-10-01 12:34:56.789')}

\item{calibration_stdtol_min}{Minimum standard deviation tolerance (g) for detecting stationary periods for calibration. Default: None}

\item{plot_activity}{Plot the predicted activity labels}

\item{cache_classifier}{Download and cache classifier file and model modules for offline usage}

\item{verbose}{print diagnostic messages}
}
\value{
A list of the results (\code{data.frame}),
summary of the results, adjusted summary of the results, and
information about the data.
}
\description{
Run Actinet Model on Data
}
\examples{
\donttest{
  library(magrittr)
  file = system.file("extdata/P30_wrist100.csv.gz", package = "actinet")
  if (actinet_check()) {
    out = actinet(file = file)
    st = out$step_times
  }
}

}
